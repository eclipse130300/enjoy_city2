using System;
using System.Text;
using UnityEngine;

namespace CMS.Configs.Audio.Models
{
/*    [Serializable]
    public class GlobalSoundSettingModel : ISaveble
    {
        public bool isMuted = false;
        [Range(-80f, 20f), Space(5)] public float globalVolume = 1f;
        [Range(-80f, 20f), Space(5)] public float musicVolume = 1f;
        [Range(-80f, 20f), Space(5)] public float uiVolume = 1f;
        [Range(-80f, 20f), Space(5)] public float ambientVolume = 1f;
        [Range(-80f, 20f), Space(5)] public float fxVolume = 1f;

        public GlobalSoundSettingModel Clone()
        {
            return (GlobalSoundSettingModel) MemberwiseClone();
        }

        public override string ToString()
        {
            return new StringBuilder()
                .AppendLine("GlobalSoundSettingModel")
                .AppendLine("isMuted : " + isMuted)
                .AppendLine("globalVolume : " + globalVolume)
                .AppendLine("musicVolume : " + musicVolume)
                .AppendLine("uiVolume : " + uiVolume)
                .AppendLine("ambientVolume : " + ambientVolume)
                .AppendLine("fxVolume : " + fxVolume)
                .ToString();
        }
    }*/
}