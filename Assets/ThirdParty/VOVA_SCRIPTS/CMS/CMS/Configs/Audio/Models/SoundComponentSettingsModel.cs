using System;
using System.Text;
using UnityEngine;

namespace CMS.Configs.Audio.Models
{
/*    using Game.Audio;

    [Serializable]
    public class SoundComponentSettingsModel
    {
        [HideInInspector] public SoundSettingsModel[] sounds = null;
        public SoundGroupType soundGroup;
        public bool isMuted;
        public bool isPlayRandom;

        public SoundComponentSettingsModel Clone()
        {
            return (SoundComponentSettingsModel) MemberwiseClone();
        }

        public override string ToString()
        {
            var stringBuilder = new StringBuilder().AppendLine("SoundComponentSettingsModel").AppendLine();
            if (sounds.IsNullOrEmpty() == false)
            {
                foreach (var item in sounds)
                {
                    stringBuilder.AppendLine(item.ToString());
                }
            }
            stringBuilder
                .AppendLine("soundGroup : " + soundGroup)
                .AppendLine("isMuted : " + isMuted)
                .AppendLine("isPlayRandom : " + isPlayRandom);

            return stringBuilder.ToString();
        }
    }*/
}